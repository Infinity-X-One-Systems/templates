# Accessibility

How the agent can be accessed and invoked.

See [`manifest.json`](manifest.json) for interfaces including `rest-api`, `openai-compatible`, `websocket`, `cli`, `webhook`, `github-actions`, `scheduled`, `event-driven`, and more.
