# Blueprints

Pre-wired agent configurations (role + capability bundles) ready to deploy.

See [`manifest.json`](manifest.json) for blueprints including `research-analyst`, `code-builder`, `sales-qualifier`, `content-creator`, `financial-analyst`, and `devops-guardian`.
