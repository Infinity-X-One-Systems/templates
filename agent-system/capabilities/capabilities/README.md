# Capabilities

Core computational capabilities that define what an agent CAN do.

See [`manifest.json`](manifest.json) for the full list including `reasoning`, `planning`, `summarization`, `classification`, `extraction`, `generation`, `orchestration`, and more.
