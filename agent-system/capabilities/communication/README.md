# Communication

How the agent communicates results and coordinates with others.

See [`manifest.json`](manifest.json) for channels including `slack-notify`, `github-pr`, `google-docs-report`, `webhook-outbound`, `repository-dispatch`, `dashboard-update`, and more.
