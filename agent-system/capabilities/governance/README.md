# Governance

Governance rules that constrain agent behavior.

See [`manifest.json`](manifest.json) for rules including `tap-protocol`, `cost-budget`, `human-approval`, `secret-guard`, `audit-log`, `rollback-ready`, `bounded-autonomy`, and more.
